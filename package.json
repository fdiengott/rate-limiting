{
  "name": "rate-limiting",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "slowdown": "bun run slowdown.ts",
    "limit": "bun run limit.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "express-slow-down": "^2.0.1"
  }
}
